export default interface Country_BDM {
    persistenceId?: number,
    persistenceId_string?: string,
    name?: string,
    description?: string,
    persistenceVersion?: number,
    persistenceVersion_string?: string,
    isActive?: boolean,
}